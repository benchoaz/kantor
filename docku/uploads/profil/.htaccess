# Prevent direct access to profile photos without proper authentication
# Photos should be served through PHP scripts that check user permissions

<Files ~ "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</Files>
